#include <stdio.h>
#include <stdlib.h>
int numeros[20];
void pageDigits(int);
int main(){
  int bandera=1;
  int contador=0
  while(bandera!=0){
    bandera=scanf("%d",&numeros[contador]);
    contador++;
  }
}
void pageDigits(int numPagina){


}
